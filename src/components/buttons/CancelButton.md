An unassuming cancel button. Pass in your own callback function to handle the cancel action.

```js static
import { CancelButton } from "nulib-admin-ui-components";
```

```jsx padded
<CancelButton onClick={() => alert("Cancel button callback function")} />
```
